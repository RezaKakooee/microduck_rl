<mujoco model="scene_egg">
    <!-- scene.xml plus an egg to balance on the duck's head (scripts/task_egg_on_head.py).

         The robot file is robot_allcollisions_egg.xml: robot_allcollisions.xml
         plus a flat 30 mm pad on the crown of the head (geom "head_pad"). The
         bare shell top is a dome; the task script can switch the pad off at
         runtime (contype/conaffinity = 0) to test the egg on the bare shell.

         Egg: an ellipsoid, 20 x 20 x 28 mm, 25 g, on a free joint. It starts
         parked on the floor; the script lifts it onto the pad once the duck
         has settled. condim 6 enables rolling friction. MuJoCo's rolling
         friction is a creep, not a static hold: measured on a static tilted
         pad, 0.0002 (a physical rubber value) lets the egg roll freely at any
         tilt, 0.003 makes it creep ~3 mm/s at 0.5 deg and ~6 mm/s at 1 deg.
         0.003 is used so the egg holds roughly as a real egg on rubber does;
         task_egg_on_head.py --rolling overrides it at runtime.

         No keyframes: the egg freejoint changes nq, which would break
         scene.xml's keyframe qpos sizes; duck_sim.make_policy sets qpos. -->
    <include file="robot_allcollisions_egg.xml" />

    <visual>
        <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0" />
        <rgba haze="0.15 0.25 0.35 1" />
        <global azimuth="160" elevation="-20" />
    </visual>

    <asset>
        <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512"
            height="3072" />
        <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4"
            rgb2="0.1 0.2 0.3"
            markrgb="0.8 0.8 0.8" width="300" height="300" />
        <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5"
            reflectance="0.2" />
    </asset>

    <worldbody>
        <light pos="0 0 3.5" dir="0 0 -1" directional="true" />
        <geom name="floor" size="0 0 0.05" pos="0 0 0" type="plane" material="groundplane" />
        <body name="egg" pos="-0.5 0.5 0.011">
            <freejoint name="egg_free"/>
            <geom name="egg_geom" type="ellipsoid" size="0.010 0.010 0.014" mass="0.025"
                  rgba="0.98 0.90 0.60 1" friction="1.2 0.005 0.003" condim="6"/>
        </body>
    </worldbody>
</mujoco>
